# Agent notes

Cross-cutting agent preferences (e.g. fine-grained commits) live in
`darwin/.cursor/rules/` and are symlinked into `~/.claude/CLAUDE.md` and
`~/.cursor/rules/` via home-manager. Prefer editing those rule files, not
pasting into Cursor Settings → User Rules.
